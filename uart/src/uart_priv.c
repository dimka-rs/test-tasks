#include <stdlib.h>
#include <string.h>

#include "uart.h"
#include "uart_priv.h"

int uart_start_tx(struct uart *uart)
{
    return UART_OK;
}

int
uart_enable_hw(struct uart *uart)
{
    return UART_OK;
}

